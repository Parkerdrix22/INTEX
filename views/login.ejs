<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Login - Ella Rises</title>
  <!-- Google Fonts - DM Serif Display -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --er-pink: #ffeef5;
      --er-pink-soft: #ffd6e8;
      --er-pink-dark: #ff9fc4;
      --er-text: #222222;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, var(--er-pink) 0%, #ffffff 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem 1rem;
    }

    /* DM Serif Display for all titles and headers */
    h1, h2, h3, h4, h5, h6,
    .logo-section h1,
    .login-title,
    .form-title {
      font-family: 'DM Serif Display', serif;
    }

    .main-container {
      width: 100%;
      max-width: 1200px;
      display: flex;
      gap: 2rem;
      background-color: #ffffff;
      border-radius: 1.5rem;
      box-shadow: 0 0.5rem 2rem rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .left-section {
      flex: 1;
      padding: 3rem;
      min-height: 600px;
      display: flex;
      flex-direction: column;
    }

    .right-section {
      width: 40%;
      background-color: var(--er-pink);
      padding: 3rem;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 2rem;
      border-radius: 1rem;
    }

    .right-section h1 {
      color: #000000;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-size: 2.5rem;
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .right-section img {
      height: 200px;
      width: auto;
      display: block;
      margin: 0 auto;
    }

    .back-button {
      display: inline-flex;
      align-items: center;
      color: var(--er-text);
      text-decoration: none;
      font-size: 0.9rem;
      margin-bottom: 1.5rem;
      transition: color 0.3s ease;
    }

    .back-button:hover {
      color: var(--er-pink-dark);
    }

    .back-button::before {
      content: "← ";
      margin-right: 0.25rem;
    }

    .tab-container {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 2rem;
      font-size: 1.1rem;
    }

    .tab-button {
      background: none;
      border: none;
      color: var(--er-text);
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      padding: 0.5rem 0;
      transition: color 0.3s ease;
      font-family: inherit;
    }

    .tab-button:hover {
      color: var(--er-pink-dark);
    }

    .tab-button.active {
      color: var(--er-pink-dark);
      border-bottom: 2px solid var(--er-pink-dark);
    }

    .tab-separator {
      color: #ccc;
    }

    .form-container {
      flex: 1;
    }

    .form-section {
      display: none;
    }

    .form-section.active {
      display: block;
    }

    .form-title {
      font-weight: 600;
      color: var(--er-text);
      margin-bottom: 2rem;
      font-size: 1.8rem;
    }

    .form-group {
      margin-bottom: 1.25rem;
    }

    .form-label {
      display: block;
      font-weight: 500;
      color: var(--er-text);
      margin-bottom: 0.5rem;
      font-size: 0.95rem;
    }

    .form-control {
      width: 100%;
      border: 2px solid #e0e0e0;
      border-radius: 0.5rem;
      padding: 0.75rem 1rem;
      font-size: 1rem;
      transition: all 0.3s ease;
      background-color: #ffffff;
      color: var(--er-text);
      font-family: inherit;
    }

    .form-control:hover {
      border-color: var(--er-pink-soft);
      background-color: #ffffff;
    }

    .form-control:focus {
      border-color: var(--er-pink-soft);
      background-color: #ffffff;
      outline: none;
    }

    .form-control:-webkit-autofill,
    .form-control:-webkit-autofill:hover,
    .form-control:-webkit-autofill:focus {
      -webkit-box-shadow: 0 0 0px 1000px #ffffff inset;
      -webkit-text-fill-color: var(--er-text);
      border: 2px solid var(--er-pink-soft);
    }

    .btn {
      width: 100%;
      background-color: var(--er-pink-dark);
      border: none;
      border-radius: 0.5rem;
      padding: 0.75rem;
      font-weight: 600;
      font-size: 1rem;
      color: #ffffff;
      cursor: pointer;
      transition: all 0.3s ease;
      font-family: inherit;
    }

    .btn:hover {
      background-color: var(--er-pink-soft);
      color: var(--er-text);
    }

    .btn:active {
      transform: scale(0.98);
    }

    .error-message {
      background-color: #fee;
      border: 1px solid #fcc;
      border-radius: 0.5rem;
      padding: 1rem;
      margin-bottom: 1.5rem;
      color: #c33;
      text-align: center;
      font-size: 0.95rem;
    }

    .success-message {
      background-color: #efe;
      border: 1px solid #cfc;
      border-radius: 0.5rem;
      padding: 1rem;
      margin-bottom: 1.5rem;
      color: #3c3;
      text-align: center;
      font-size: 0.95rem;
    }

    @media (max-width: 968px) {
      .main-container {
        flex-direction: column;
      }

      .right-section {
        width: 100%;
        margin: 0;
        padding: 2rem;
        border-radius: 0;
      }

      .left-section {
        padding: 2rem;
      }
    }

    @media (max-width: 480px) {
      body {
        padding: 1rem 0.5rem;
      }

      .left-section {
        padding: 1.5rem;
      }

      .right-section {
        padding: 1.5rem;
      }

      .right-section h1 {
        font-size: 1.8rem;
      }
    }
  </style>
</head>
<body>
  <div class="main-container">
    <div class="left-section">
      <a href="/" class="back-button">Back to Home</a>
      
      <div class="tab-container">
        <button class="tab-button active" onclick="showLogin()">Login</button>
        <span class="tab-separator">|</span>
        <button class="tab-button" onclick="showSignup()">Sign Up</button>
      </div>

      <!-- Error/Success Messages -->
      <% if (error_message && error_message !== "") { %>
        <% const isSuccess = error_message.toLowerCase().includes('success'); %>
        <div class="<%= isSuccess ? 'success-message' : 'error-message' %>" id="error_message" name="error_message">
          <%= error_message %>
        </div>
      <% } %>

      <!-- Login Form -->
      <div id="loginForm" class="form-section active">
        <h2 class="form-title">Welcome Back</h2>
        <form action="/login" method="POST">
          <div class="form-group">
            <label for="username" class="form-label">Username</label>
            <input type="text" id="username" name="username" class="form-control" required autofocus>
          </div>
          <div class="form-group">
            <label for="password" class="form-label">Password</label>
            <input type="password" id="password" name="password" class="form-control" required>
          </div>
          <button type="submit" class="btn">Login</button>
        </form>
      </div>

      <!-- Sign Up Form -->
      <div id="signupForm" class="form-section">
        <h2 class="form-title">Create Account</h2>
        <form action="/signup" method="POST">
          <div class="form-group">
            <label for="signup-first-name" class="form-label">First Name</label>
            <input type="text" id="signup-first-name" name="first_name" class="form-control" required>
          </div>
          <div class="form-group">
            <label for="signup-last-name" class="form-label">Last Name</label>
            <input type="text" id="signup-last-name" name="last_name" class="form-control" required>
          </div>
          <div class="form-group">
            <label for="signup-username" class="form-label">Username</label>
            <input type="text" id="signup-username" name="username" class="form-control" required>
          </div>
          <div class="form-group">
            <label for="signup-password" class="form-label">Password</label>
            <input type="password" id="signup-password" name="password" class="form-control" required>
          </div>
          <button type="submit" class="btn">Sign Up</button>
        </form>
      </div>
    </div>

    <div class="right-section">
      <div>
        <h1>Ella Rises</h1>
        <img src="/images/ella_logo.avif" alt="Ella Rises Logo">
      </div>
    </div>
  </div>

  <script>
    function showLogin() {
      document.getElementById('loginForm').classList.add('active');
      document.getElementById('signupForm').classList.remove('active');
      document.querySelectorAll('.tab-button')[0].classList.add('active');
      document.querySelectorAll('.tab-button')[1].classList.remove('active');
    }

    function showSignup() {
      document.getElementById('signupForm').classList.add('active');
      document.getElementById('loginForm').classList.remove('active');
      document.querySelectorAll('.tab-button')[1].classList.add('active');
      document.querySelectorAll('.tab-button')[0].classList.remove('active');
    }
  </script>
</body>
</html>
