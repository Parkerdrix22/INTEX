<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Events - Ella Rises</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts - DM Serif Display -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&display=swap" rel="stylesheet">
    <style>
        :root {
            --er-pink: #ffeef5;
            --er-pink-dark: #ff9fc4;
            --er-text: #222222;
        }

        body {
            background-color: #ffffff;
            color: var(--er-text);
        }

        /* DM Serif Display for all titles and headers */
        h1, h2, h3, h4, h5, h6,
        .navbar-brand {
            font-family: 'DM Serif Display', serif;
        }

        /* Light pink navbar */
        .navbar {
            background-color: var(--er-pink) !important;
            padding: 1.2rem 0;
            min-height: 70px;
        }

        .navbar .container {
            padding-left: 1rem;
        }

        .navbar-brand {
            font-weight: 700;
            letter-spacing: 0.06em;
            text-transform: uppercase;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-left: 0 !important;
            padding-left: 0 !important;
        }

        .navbar-brand img {
            height: 60px;
            width: auto;
        }

        .nav-link {
            font-weight: 500;
            margin: 0 0.75rem;
        }

        .nav-link.sign-in-link {
            margin-left: 2rem;
            padding-left: 1.5rem;
            border-left: 2px solid rgba(0, 0, 0, 0.1);
        }

        /* Footer */
        footer {
            background-color: var(--er-pink);
            padding: 3rem 1rem 2rem;
            margin-top: 4rem;
            border-top: 1px solid rgba(0, 0, 0, 0.1);
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
        }

        .footer-section {
            margin-bottom: 2rem;
        }

        .footer-section h4 {
            font-weight: 700;
            font-size: 1.1rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .footer-section p,
        .footer-section a {
            color: var(--er-text);
            text-decoration: none;
            font-size: 0.95rem;
            line-height: 1.6;
        }

        .footer-section a:hover {
            color: var(--er-pink-dark);
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 1rem;
        }

        .footer-logo img {
            height: 40px;
            width: auto;
        }

        .footer-logo span {
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            font-size: 1.2rem;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 2rem;
            border-top: 1px solid rgba(0, 0, 0, 0.1);
            margin-top: 2rem;
            color: #666;
            font-size: 0.9rem;
        }

        @media (max-width: 768px) {
            .footer-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Header Section */
        .events-header {
            background-image: url('/images/event_header.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            padding: 6rem 2rem;
            text-align: center;
            margin-bottom: 3rem;
            position: relative;
            min-height: 400px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .events-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.6);
            z-index: 1;
        }

        .events-header h1,
        .events-header p {
            position: relative;
            z-index: 2;
        }

        .header-image-placeholder {
            display: none;
        }

        .events-header h1 {
            font-size: 3.5rem;
            font-weight: 800;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            color: #ffffff;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .events-header p {
            font-size: 1.2rem;
            color: #ffffff;
            max-width: 600px;
            margin: 0 auto;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }

        /* Events Filters */
        .events-filters {
            max-width: 1200px;
            margin: 0 auto 2rem;
            padding: 2rem;
            background-color: #fff;
            border-radius: 1rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .filters-title {
            font-family: 'DM Serif Display', serif;
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--er-text);
        }

        .filters-row {
            display: flex;
            flex-wrap: wrap;
            gap: 1.5rem;
            align-items: center;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .filter-group label {
            font-size: 0.9rem;
            font-weight: 600;
            color: #666;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .filter-group select,
        .filter-group input {
            padding: 0.75rem 1rem;
            border: 2px solid #e0e0e0;
            border-radius: 0.5rem;
            font-size: 1rem;
            background-color: #fff;
            color: var(--er-text);
            transition: border-color 0.3s ease;
            min-width: 180px;
        }

        .filter-group select:hover,
        .filter-group select:focus,
        .filter-group input:hover,
        .filter-group input:focus {
            outline: none;
            border-color: var(--er-pink-dark);
        }

        .filter-buttons {
            display: flex;
            gap: 1rem;
            margin-left: auto;
        }

        .filter-btn {
            padding: 0.75rem 1.5rem;
            border: 2px solid var(--er-pink-dark);
            border-radius: 0.5rem;
            background-color: #fff;
            color: var(--er-pink-dark);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .filter-btn:hover {
            background-color: var(--er-pink-dark);
            color: #fff;
        }

        .filter-btn.active {
            background-color: var(--er-pink-dark);
            color: #fff;
        }

        /* Manager Controls */
        .manager-controls {
            max-width: 1200px;
            margin: 0 auto 2rem;
            padding: 0 2rem;
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
        }

        .manager-btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 0.5rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1rem;
        }

        .manager-btn-add {
            background-color: var(--er-pink-dark);
            color: #fff;
        }

        .manager-btn-add:hover {
            background-color: #ff8ab8;
        }

        .manager-btn-edit {
            background-color: #4a90e2;
            color: #fff;
        }

        .manager-btn-edit:hover {
            background-color: #357abd;
        }

        .manager-btn-delete {
            background-color: #e74c3c;
            color: #fff;
        }

        .manager-btn-delete:hover {
            background-color: #c0392b;
        }

        .event-card-wrapper {
            position: relative;
            display: block;
        }

        .event-card {
            position: relative;
        }

        .event-card-edit-btn,
        .event-card-delete-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: rgba(255, 255, 255, 0.9);
            border: none;
            border-radius: 50%;
            width: 35px;
            height: 35px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            z-index: 10;
            font-size: 0.9rem;
        }

        .event-card-edit-btn {
            right: 50px;
            color: #4a90e2;
        }

        .event-card-edit-btn:hover {
            background-color: #4a90e2;
            color: #fff;
        }

        .event-card-delete-btn {
            color: #e74c3c;
        }

        .event-card-delete-btn:hover {
            background-color: #e74c3c;
            color: #fff;
        }

        /* Modal Styles */
        .event-modal .modal-dialog {
            max-width: 700px;
            width: 90%;
        }

        .event-modal .modal-content {
            padding: 0;
        }

        .event-modal .modal-header {
            background-color: var(--er-pink);
            border-bottom: 2px solid var(--er-pink-dark);
            padding: 1.5rem 2rem;
        }

        .event-modal .modal-title {
            font-family: 'DM Serif Display', serif;
            color: var(--er-text);
            font-size: 1.75rem;
        }

        .event-modal .modal-body {
            padding: 2rem;
        }

        .event-modal .form-label {
            font-weight: 600;
            color: var(--er-text);
            margin-top: 1rem;
            margin-bottom: 0.5rem;
        }

        .event-modal .form-control,
        .event-modal .form-select {
            border: 2px solid #e0e0e0;
            border-radius: 0.5rem;
            padding: 0.875rem 1rem;
            font-size: 1rem;
        }

        .event-modal .form-control:focus,
        .event-modal .form-select:focus {
            border-color: var(--er-pink-dark);
            outline: none;
            box-shadow: 0 0 0 0.2rem rgba(255, 159, 196, 0.25);
        }

        .event-modal .modal-footer {
            padding: 1.5rem 2rem;
            border-top: 1px solid #e0e0e0;
        }

        .event-modal .btn-primary {
            background-color: var(--er-pink-dark);
            border-color: var(--er-pink-dark);
            padding: 0.75rem 2rem;
        }

        .event-modal .btn-primary:hover {
            background-color: #ff8ab8;
            border-color: #ff8ab8;
        }

        .event-modal .btn-secondary {
            padding: 0.75rem 2rem;
        }

        /* Events Grid */
        .events-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem 4rem;
        }

        .events-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
        }

        .event-card {
            background-color: #fff;
            border-radius: 1rem;
            overflow: hidden;
            box-shadow: 0 0.25rem 0.75rem rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            display: block;
        }

        .event-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.15);
        }

        .event-image {
            width: 100%;
            height: 200px;
            background-color: var(--er-pink);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #999;
            font-style: italic;
        }

        .event-content {
            padding: 1.5rem;
        }

        .event-name {
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            color: var(--er-text);
        }

        .event-description {
            font-size: 0.95rem;
            color: #666;
            line-height: 1.6;
            margin-bottom: 0.5rem;
            display: flex;
            gap: 1rem;
        }

        .event-date {
            font-size: 0.85rem;
            color: var(--er-pink-dark);
            font-weight: 600;
            flex-shrink: 0;
            min-width: 120px;
        }

        .event-description-text {
            flex: 1;
        }

        @media (max-width: 992px) {
            .events-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .events-header h1 {
                font-size: 2.5rem;
            }

            .events-header {
                padding: 3rem 1.5rem;
                min-height: 300px;
            }

            .events-filters {
                padding: 1.5rem;
                margin: 0 1rem 2rem;
            }

            .filters-row {
                flex-direction: column;
                align-items: stretch;
            }

            .filter-group {
                width: 100%;
            }

            .filter-group select,
            .filter-group input {
                width: 100%;
                min-width: auto;
            }

            .filter-buttons {
                margin-left: 0;
                margin-top: 1rem;
            }

            .filter-btn {
                flex: 1;
            }

            .events-grid {
                grid-template-columns: 1fr;
            }

            .events-container {
                padding: 0 1rem 3rem;
            }

            .manager-controls {
                padding: 0 1rem;
                justify-content: center;
            }

            .manager-btn {
                font-size: 0.9rem;
                padding: 0.6rem 1.2rem;
            }

            .event-card-edit-btn,
            .event-card-delete-btn {
                width: 30px;
                height: 30px;
                font-size: 0.8rem;
            }

            .event-card-edit-btn {
                right: 45px;
            }
        }
    </style>
</head>
<body>
    <!-- Light pink navbar with links -->
    <nav class="navbar navbar-expand-md navbar-light">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/images/ella_logo.avif" alt="Ella Rises Logo">
                <span>Ella Rises</span>
            </a>
            <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navContent"
                aria-controls="navContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navContent">
                <ul class="navbar-nav ms-auto mb-2 mb-md-0">
                    <% if (user && user.isManager) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/#participants">Participants</a>
                    </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link" href="/#events">Events</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/#surveys">Event Feedback</a>
                    </li>
                    <% if (user && user.isManager) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/#milestones">Milestones</a>
                    </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link" href="/#donations">Donations</a>
                    </li>
                    <% if (user && user.isManager) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/#dashboard">Dashboard</a>
                    </li>
                    <% } %>
                    <% if (!user) { %>
                    <li class="nav-item">
                        <a class="nav-link sign-in-link" href="/login">Sign In</a>
                    </li>
                    <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link sign-in-link" href="/logout">Logout</a>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Events Header -->
    <div class="events-header">
        <h1>EVENTS</h1>
        <p>Join us for inspiring workshops, creative experiences, and leadership opportunities that empower the rising generation of women in STEAM fields.</p>
    </div>

    <!-- Events Filters -->
    <div class="events-filters">
        <div class="filters-title">Filter Events</div>
        <div class="filters-row">
            <div class="filter-group">
                <label for="event-type">Event Type</label>
                <select id="event-type" class="event-filter">
                    <option value="all">All Events</option>
                    <option value="workshop">Workshops</option>
                    <option value="mentoring">Mentoring</option>
                    <option value="creative">Creative Experiences</option>
                    <option value="leadership">Leadership</option>
                </select>
            </div>
            <div class="filter-group">
                <label for="event-date">Date</label>
                <select id="event-date" class="event-filter">
                    <option value="all">All Dates</option>
                    <option value="upcoming">Upcoming</option>
                    <option value="past">Past Events</option>
                    <option value="this-month">This Month</option>
                </select>
            </div>
            <div class="filter-group">
                <label for="event-category">Category</label>
                <select id="event-category" class="event-filter">
                    <option value="all">All Categories</option>
                    <option value="steam">STEAM</option>
                    <option value="arts">Arts</option>
                    <option value="technology">Technology</option>
                    <option value="engineering">Engineering</option>
                </select>
            </div>
            <div class="filter-buttons">
                <button class="filter-btn" onclick="clearFilters()">Clear Filters</button>
            </div>
        </div>
    </div>

    <!-- Manager Controls -->
    <% if (user && user.isManager) { %>
    <div class="manager-controls">
        <button class="manager-btn manager-btn-add" onclick="openAddEventModal()">
            + Add Event
        </button>
    </div>
    <% } %>

    <!-- Events Grid -->
    <div class="events-container">
        <div class="events-grid">
            <!-- Event Card 1 -->
            <div class="event-card-wrapper" data-event-id="1" data-type="workshop" data-category="steam" data-date="upcoming">
                <% if (user && user.isManager) { %>
                <button class="event-card-edit-btn" onclick="openEditEventModal(1)" title="Edit Event">✎</button>
                <button class="event-card-delete-btn" onclick="confirmDeleteEvent(1, 'STEAM Workshop: Coding Basics')" title="Delete Event">×</button>
                <% } %>
                <a href="/rsvp" class="event-card">
                    <div class="event-image">Image placeholder</div>
                    <div class="event-content">
                        <div class="event-name">STEAM Workshop: Coding Basics</div>
                        <div class="event-description">
                            <span class="event-date">March 15, 2025</span>
                            <span class="event-description-text">Learn the fundamentals of programming in a supportive environment designed for beginners.</span>
                        </div>
                    </div>
                </a>
            </div>

            <!-- Event Card 2 -->
            <div class="event-card-wrapper" data-event-id="2" data-type="creative" data-category="arts" data-date="upcoming">
                <% if (user && user.isManager) { %>
                <button class="event-card-edit-btn" onclick="openEditEventModal(2)" title="Edit Event">✎</button>
                <button class="event-card-delete-btn" onclick="confirmDeleteEvent(2, 'Creative Design Lab')" title="Delete Event">×</button>
                <% } %>
                <a href="/rsvp" class="event-card">
                    <div class="event-image">Image placeholder</div>
                    <div class="event-content">
                        <div class="event-name">Creative Design Lab</div>
                        <div class="event-description">
                            <span class="event-date">March 22, 2025</span>
                            <span class="event-description-text">Explore your artistic side while learning design principles and digital tools.</span>
                        </div>
                    </div>
                </a>
            </div>

            <!-- Event Card 3 -->
            <div class="event-card-wrapper" data-event-id="3" data-type="leadership" data-category="steam" data-date="upcoming">
                <% if (user && user.isManager) { %>
                <button class="event-card-edit-btn" onclick="openEditEventModal(3)" title="Edit Event">✎</button>
                <button class="event-card-delete-btn" onclick="confirmDeleteEvent(3, 'Leadership Circle')" title="Delete Event">×</button>
                <% } %>
                <a href="/rsvp" class="event-card">
                    <div class="event-image">Image placeholder</div>
                    <div class="event-content">
                        <div class="event-name">Leadership Circle</div>
                        <div class="event-description">
                            <span class="event-date">April 5, 2025</span>
                            <span class="event-description-text">Build confidence and leadership skills through mentorship and collaborative activities.</span>
                        </div>
                    </div>
                </a>
            </div>

            <!-- Event Card 4 -->
            <div class="event-card-wrapper" data-event-id="4" data-type="workshop" data-category="engineering" data-date="upcoming">
                <% if (user && user.isManager) { %>
                <button class="event-card-edit-btn" onclick="openEditEventModal(4)" title="Edit Event">✎</button>
                <button class="event-card-delete-btn" onclick="confirmDeleteEvent(4, 'Engineering Challenge')" title="Delete Event">×</button>
                <% } %>
                <a href="/rsvp" class="event-card">
                    <div class="event-image">Image placeholder</div>
                    <div class="event-content">
                        <div class="event-name">Engineering Challenge</div>
                        <div class="event-description">
                            <span class="event-date">April 12, 2025</span>
                            <span class="event-description-text">Hands-on engineering projects that combine creativity with technical problem-solving.</span>
                        </div>
                    </div>
                </a>
            </div>

            <!-- Event Card 5 -->
            <div class="event-card-wrapper" data-event-id="5" data-type="creative" data-category="arts" data-date="upcoming">
                <% if (user && user.isManager) { %>
                <button class="event-card-edit-btn" onclick="openEditEventModal(5)" title="Edit Event">✎</button>
                <button class="event-card-delete-btn" onclick="confirmDeleteEvent(5, 'Math & Art Fusion')" title="Delete Event">×</button>
                <% } %>
                <a href="/rsvp" class="event-card">
                    <div class="event-image">Image placeholder</div>
                    <div class="event-content">
                        <div class="event-name">Math & Art Fusion</div>
                        <div class="event-description">
                            <span class="event-date">April 19, 2025</span>
                            <span class="event-description-text">Discover the beautiful connection between mathematics and artistic expression.</span>
                        </div>
                    </div>
                </a>
            </div>

            <!-- Event Card 6 -->
            <div class="event-card-wrapper" data-event-id="6" data-type="mentoring" data-category="steam" data-date="upcoming">
                <% if (user && user.isManager) { %>
                <button class="event-card-edit-btn" onclick="openEditEventModal(6)" title="Edit Event">✎</button>
                <button class="event-card-delete-btn" onclick="confirmDeleteEvent(6, 'Career Exploration Day')" title="Delete Event">×</button>
                <% } %>
                <a href="/rsvp" class="event-card">
                    <div class="event-image">Image placeholder</div>
                    <div class="event-content">
                        <div class="event-name">Career Exploration Day</div>
                        <div class="event-description">
                            <span class="event-date">May 3, 2025</span>
                            <span class="event-description-text">Meet professionals in STEAM fields and learn about exciting career opportunities.</span>
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </div>

    <!-- Event Modal -->
    <% if (user && user.isManager) { %>
    <div class="modal fade event-modal" id="eventModal" tabindex="-1" aria-labelledby="eventModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="eventModalTitle">Add New Event</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="eventForm" onsubmit="handleEventSubmit(event)">
                        <div class="mb-3">
                            <label for="eventName" class="form-label">Event Name</label>
                            <input type="text" class="form-control" id="eventName" required>
                        </div>
                        <div class="mb-3">
                            <label for="eventDate" class="form-label">Event Date</label>
                            <input type="text" class="form-control" id="eventDate" placeholder="e.g., March 15, 2025" required>
                        </div>
                        <div class="mb-3">
                            <label for="modalEventType" class="form-label">Event Type</label>
                            <select class="form-select" id="modalEventType" required>
                                <option value="workshop">Workshop</option>
                                <option value="mentoring">Mentoring</option>
                                <option value="creative">Creative Experience</option>
                                <option value="leadership">Leadership</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="modalEventCategory" class="form-label">Category</label>
                            <select class="form-select" id="modalEventCategory" required>
                                <option value="steam">STEAM</option>
                                <option value="arts">Arts</option>
                                <option value="technology">Technology</option>
                                <option value="engineering">Engineering</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="eventDescription" class="form-label">Description</label>
                            <textarea class="form-control" id="eventDescription" rows="4" required></textarea>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-primary">Save Event</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <% } %>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="footer-grid">
                <div class="footer-section">
                    <div class="footer-logo">
                        <img src="/images/ella_logo.avif" alt="Ella Rises Logo">
                        <span>Ella Rises</span>
                    </div>
                    <p>
                        Empowering the rising generation of women to pursue higher education 
                        and embrace their heritage through STEAM programs, mentoring, and 
                        leadership opportunities.
                    </p>
                </div>
                
                <div class="footer-section">
                    <h4>Quick Links</h4>
                    <p><a href="/#events">Events</a></p>
                    <p><a href="/#surveys">Event Feedback</a></p>
                    <p><a href="/#donations">Donations</a></p>
                </div>
                
                <div class="footer-section">
                    <h4>Contact</h4>
                    <p>Visit us at:</p>
                    <p><a href="https://www.ellarises.org/" target="_blank">www.ellarises.org</a></p>
                    <p style="margin-top: 1rem;">
                        Partnerships with UVU and BYU
                    </p>
                </div>
            </div>
            
            <div class="footer-bottom">
                <p>&copy; 2025 Ella Rises. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Set manager status (server-side variable) -->
    <script>
        var isManager = <%= (user && user.isManager) ? 'true' : 'false' %>;
    </script>

    <!-- Filter Functionality -->
    <script>

        // Load persisted events data from localStorage
        function loadPersistedEvents() {
            const eventsData = localStorage.getItem('eventsData');
            if (eventsData) {
                return JSON.parse(eventsData);
            }
            return null;
        }

        function savePersistedEvents(events) {
            localStorage.setItem('eventsData', JSON.stringify(events));
        }

        function getEventDataFromDOM() {
            const events = [];
            document.querySelectorAll('.event-card-wrapper').forEach(wrapper => {
                const eventId = wrapper.getAttribute('data-event-id');
                const eventName = wrapper.querySelector('.event-name')?.textContent || '';
                const eventDate = wrapper.querySelector('.event-date')?.textContent || '';
                const eventDescription = wrapper.querySelector('.event-description-text')?.textContent || '';
                const eventType = wrapper.getAttribute('data-type') || '';
                const eventCategory = wrapper.getAttribute('data-category') || '';
                
                events.push({
                    id: eventId,
                    name: eventName,
                    date: eventDate,
                    description: eventDescription,
                    type: eventType,
                    category: eventCategory
                });
            });
            return events;
        }

        function createEventCardHTML(event, eventId) {
            const escapedName = event.name.replace(/'/g, "\\'").replace(/"/g, '&quot;');
            if (isManager) {
                return `
                    <button class="event-card-edit-btn" onclick="openEditEventModal(${eventId})" title="Edit Event">✎</button>
                    <button class="event-card-delete-btn" onclick="confirmDeleteEvent(${eventId}, '${escapedName}')" title="Delete Event">×</button>
                    <a href="/rsvp" class="event-card">
                        <div class="event-image">Image placeholder</div>
                        <div class="event-content">
                            <div class="event-name">${event.name}</div>
                            <div class="event-description">
                                <span class="event-date">${event.date}</span>
                                <span class="event-description-text">${event.description}</span>
                            </div>
                        </div>
                    </a>
                `;
            } else {
                return `
                    <a href="/rsvp" class="event-card">
                        <div class="event-image">Image placeholder</div>
                        <div class="event-content">
                            <div class="event-name">${event.name}</div>
                            <div class="event-description">
                                <span class="event-date">${event.date}</span>
                                <span class="event-description-text">${event.description}</span>
                            </div>
                        </div>
                    </a>
                `;
            }
        }

        function applyPersistedEvents() {
            const persistedEvents = loadPersistedEvents();
            if (!persistedEvents) return;

            const eventsGrid = document.querySelector('.events-grid');
            if (!eventsGrid) return;

            // Remove all existing events
            document.querySelectorAll('.event-card-wrapper').forEach(wrapper => {
                wrapper.remove();
            });

            // Rebuild events from persisted data
            persistedEvents.forEach(event => {
                const eventId = event.id;
                const wrapper = document.createElement('div');
                wrapper.className = 'event-card-wrapper';
                wrapper.setAttribute('data-event-id', eventId);
                wrapper.setAttribute('data-type', event.type);
                wrapper.setAttribute('data-category', event.category);
                wrapper.setAttribute('data-date', 'upcoming');
                wrapper.innerHTML = createEventCardHTML(event, eventId);
                eventsGrid.appendChild(wrapper);
            });
        }

        document.addEventListener('DOMContentLoaded', function() {
            // Apply persisted events if they exist
            const persistedEvents = loadPersistedEvents();
            if (persistedEvents && persistedEvents.length > 0) {
                applyPersistedEvents();
            } else {
                // Save initial events to localStorage on first load
                const initialEvents = getEventDataFromDOM();
                if (initialEvents && initialEvents.length > 0) {
                    savePersistedEvents(initialEvents);
                }
            }

            const eventTypeFilter = document.getElementById('event-type');
            const eventDateFilter = document.getElementById('event-date');
            const eventCategoryFilter = document.getElementById('event-category');
            const eventCardWrappers = document.querySelectorAll('.event-card-wrapper');

            function filterEvents() {
                const typeValue = eventTypeFilter.value;
                const dateValue = eventDateFilter.value;
                const categoryValue = eventCategoryFilter.value;

                eventCardWrappers.forEach(wrapper => {
                    const cardType = wrapper.getAttribute('data-type');
                    const cardDate = wrapper.getAttribute('data-date');
                    const cardCategory = wrapper.getAttribute('data-category');

                    let showCard = true;

                    // Filter by type
                    if (typeValue !== 'all' && cardType !== typeValue) {
                        showCard = false;
                    }

                    // Filter by date
                    if (dateValue !== 'all' && cardDate !== dateValue) {
                        showCard = false;
                    }

                    // Filter by category
                    if (categoryValue !== 'all' && cardCategory !== categoryValue) {
                        showCard = false;
                    }

                    // Show or hide card
                    if (showCard) {
                        wrapper.style.display = 'block';
                    } else {
                        wrapper.style.display = 'none';
                    }
                });
            }

            // Add event listeners
            eventTypeFilter.addEventListener('change', filterEvents);
            eventDateFilter.addEventListener('change', filterEvents);
            eventCategoryFilter.addEventListener('change', filterEvents);

            // Clear filters function
            window.clearFilters = function() {
                eventTypeFilter.value = 'all';
                eventDateFilter.value = 'all';
                eventCategoryFilter.value = 'all';
                filterEvents();
            };
        });

        // Manager Functions
        function openAddEventModal() {
            document.getElementById('eventModalTitle').textContent = 'Add New Event';
            document.getElementById('eventForm').reset();
            document.getElementById('eventForm').setAttribute('data-mode', 'add');
            document.getElementById('eventForm').removeAttribute('data-event-id');
            const modal = new bootstrap.Modal(document.getElementById('eventModal'));
            modal.show();
        }

        function openEditEventModal(eventId) {
            // Get event data from the card
            const wrapper = document.querySelector(`[data-event-id="${eventId}"]`);
            if (!wrapper) return;

            const eventName = wrapper.querySelector('.event-name').textContent;
            const eventDate = wrapper.querySelector('.event-date').textContent;
            const eventDescription = wrapper.querySelector('.event-description-text').textContent;
            const eventType = wrapper.getAttribute('data-type');
            const eventCategory = wrapper.getAttribute('data-category');

            // Populate form
            document.getElementById('eventModalTitle').textContent = 'Edit Event';
            document.getElementById('eventName').value = eventName;
            document.getElementById('eventDate').value = eventDate;
            document.getElementById('eventDescription').value = eventDescription;
            document.getElementById('modalEventType').value = eventType;
            document.getElementById('modalEventCategory').value = eventCategory;
            
            document.getElementById('eventForm').setAttribute('data-mode', 'edit');
            document.getElementById('eventForm').setAttribute('data-event-id', eventId);
            
            const modal = new bootstrap.Modal(document.getElementById('eventModal'));
            modal.show();
        }

        function confirmDeleteEvent(eventId, eventName) {
            if (confirm(`Are you sure you want to delete "${eventName}"?\n\nThis action cannot be undone.`)) {
                deleteEvent(eventId);
            }
        }

        function deleteEvent(eventId) {
            // Remove the event card from the DOM
            const wrapper = document.querySelector(`[data-event-id="${eventId}"]`);
            if (wrapper) {
                wrapper.style.transition = 'opacity 0.3s';
                wrapper.style.opacity = '0';
                setTimeout(() => {
                    wrapper.remove();
                    // Update persisted events
                    const events = getEventDataFromDOM();
                    savePersistedEvents(events);
                    alert('Event deleted successfully! This change will be visible to all users.');
                }, 300);
            }
        }

        function handleEventSubmit(event) {
            event.preventDefault();
            const form = event.target;
            const mode = form.getAttribute('data-mode');
            const eventId = form.getAttribute('data-event-id');

            const eventName = document.getElementById('eventName').value;
            const eventDate = document.getElementById('eventDate').value;
            const eventDescription = document.getElementById('eventDescription').value;
            const eventType = document.getElementById('modalEventType').value;
            const eventCategory = document.getElementById('modalEventCategory').value;

            if (mode === 'add') {
                // Add new event
                const eventsGrid = document.querySelector('.events-grid');
                const newEventId = Date.now(); // Generate unique ID
                
                const newEvent = {
                    name: eventName,
                    date: eventDate,
                    description: eventDescription,
                    type: eventType,
                    category: eventCategory
                };
                
                const wrapper = document.createElement('div');
                wrapper.className = 'event-card-wrapper';
                wrapper.setAttribute('data-event-id', newEventId);
                wrapper.setAttribute('data-type', eventType);
                wrapper.setAttribute('data-category', eventCategory);
                wrapper.setAttribute('data-date', 'upcoming');
                wrapper.innerHTML = createEventCardHTML(newEvent, newEventId);
                eventsGrid.appendChild(wrapper);
                
                // Save to localStorage
                const events = getEventDataFromDOM();
                savePersistedEvents(events);
                alert('Event added successfully! This change will be visible to all users.');
            } else if (mode === 'edit') {
                // Update existing event
                const wrapper = document.querySelector(`[data-event-id="${eventId}"]`);
                if (wrapper) {
                    wrapper.querySelector('.event-name').textContent = eventName;
                    wrapper.querySelector('.event-date').textContent = eventDate;
                    wrapper.querySelector('.event-description-text').textContent = eventDescription;
                    wrapper.setAttribute('data-type', eventType);
                    wrapper.setAttribute('data-category', eventCategory);
                    
                    // Save to localStorage
                    const events = getEventDataFromDOM();
                    savePersistedEvents(events);
                    alert('Event updated successfully! This change will be visible to all users.');
                }
            }

            const modal = bootstrap.Modal.getInstance(document.getElementById('eventModal'));
            modal.hide();
        }
    </script>
</body>
</html>

